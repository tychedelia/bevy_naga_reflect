[package]
name = "bevy_naga_reflect"
version = "0.2.0"
edition = "2021"

[lib]

[dependencies]
bevy = { git = "https://github.com/bevyengine/bevy", branch = "main" }
naga = { version = "29", features = ["wgsl-in", "serialize", "deserialize"] }